/**
 * No-Op Seeder
 * This file is kept as a placeholder but does not create any data automatically.
 * Full data management is now handled manually by the System Administrator.
 */
const seedDatabase = async () => {
  // No automatic data creation
  console.log('ℹ️ Database seeder is in manual mode. No automatic data created.');
};

export default seedDatabase;
